# Manual Testing notes

- Welcome screen sucks. Needs to wrap the banner and center it on the welcome screen. Recent Files should be in a container on the left, Main Options should be on the right:
  - Font label box
    - [N] [ Nerdicon ] New Font from System Font
    - [I] [Nerdicon New Font from File]
    - [B] [ Nerdicon New Blank Font ]
    - [O] [ Nerdicon Open Font ]
    - [D] [ Nerdicon Open Duplicate ]
  - Image label box
    - New Image
    - New from Template ( shows a grid of templates, start with a banner that's the standard width of a terminal, then some other common sizes to start )
    - Convert Img to ASCII
    - Open Figmap image *like a psd, layers, colors, etc

Anyways i didn't fill out the whole menu but you get the picture.

Esc shouldn't quit and be "back out" only have Q quit or ctrl C

Image Editor can't switch to mode, can't click on anything
come to think of it can't click on welcome banner stuff either.

Still no border around actual canvas, no idea where it starts and ends

so much wasted space in the pallete drawer, why not put it under the tools? that right side drawer should be props editing, settings, image/font info, text input for text tool in image editor, libraries of brushes and stamps, layers, effects, global options like font header

collapse the borders like in ratatui docs

status bar at bottom should be modeled after lazyvim neovim powerline status bar, have sections for left, right middle middle is flex full, etc. overall everytihng needs icons everywhere from nerdfont if i need to pick more just lemm know.


where's all the animation stuff and timeline, etc?
